"""Agents package.

Each agent is an autonomous component: one job, its own state, and it talks to
the rest of the system only through messages (method calls for now). The first
one is the Order Agent.
"""
